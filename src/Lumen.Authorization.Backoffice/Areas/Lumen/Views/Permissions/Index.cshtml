@model PermissionCatalogueViewModel
@{
    ViewData["Title"] = BackofficeDisplayTokens.PageTitlePermissions;
    ViewData["Subtitle"] = BackofficeDisplayTokens.PageSubtitlePermissions;
}

@await Component.InvokeAsync("PermissionCatalogue", new { model = Model })
